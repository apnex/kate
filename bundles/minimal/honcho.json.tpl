{
  "_comment": "kate/bundles/minimal — Honcho plugin DISABLED. Replaces apnex/hermes/manifests/honcho.json.tpl via kustomize merge. The minimal bundle's wanted-plugins.yaml is empty, so no plugin loads this file in normal operation; enabled:false is defensive — if someone later re-enables the honcho-tuned plugin without also updating this file, the plugin starts up disabled instead of pointing at a non-existent service.",
  "enabled": false
}
